import { TopHeader } from "./TopHeader"
import { Navbar } from "./Navbar"

export const Header = () => {
  return (
    <header>
        <TopHeader />
        <Navbar />
    </header>
  )
}

export default Header